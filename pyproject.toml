[tool.ruff]
line-length = 110

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4"]
ignore = ["E501", "E402"]  # line-length conflicts with formatter; module-level imports after path manipulation
exclude = ["**/tests/**"]
